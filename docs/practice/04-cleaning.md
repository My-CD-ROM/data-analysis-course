# Практика 4. Чищення набору даних

*Мета заняття: побудувати "брудний" набір даних із типовими дефектами
(пропуски, дублікати, неправильні типи, неузгоджені категорії, викид) і
послідовно усунути кожен дефект — на прикладі з лекції 4.*

## Варіанти

Завдання нижче виконуються на **своєму** наборі — умовний
інтернет-магазин, який продає один товар у своєму місті. Номер варіанта
— за списком групи. Таблиця дає лише базові (чисті) значення: сам
"брудний" набір ви побудуєте в Завданні 1 за єдиною для всіх
специфікацією нижче.

| Варіант | Місто | Базова ціна (грн) | Базова кількість |
|---|---|---|---|
| 1 | Київ | 850 | 3 |
| 2 | Львів | 720 | 4 |
| 3 | Одеса | 640 | 5 |
| 4 | Харків | 780 | 2 |
| 5 | Дніпро | 810 | 3 |
| 6 | Чернігів | 690 | 4 |
| 7 | Івано-Франківськ | 760 | 3 |
| 8 | Полтава | 700 | 4 |
| 9 | Суми | 730 | 2 |
| 10 | Ужгород | 670 | 5 |

## Приклад: наскрізна демонстрація

Розберіть цей приклад — брудний набір медичних вимірювань шести
пацієнтів — перш ніж будувати й чистити набір свого варіанта.

```python
import numpy as np
import pandas as pd

patients = pd.DataFrame([
    {"id": 1, "вік": 34, "вага": "70 кг", "група_крові": "A+"},
    {"id": 2, "вік": np.nan, "вага": "82 кг", "група_крові": "o-"},
    {"id": 3, "вік": 61, "вага": "65 кг", "група_крові": "B+ "},
    {"id": 3, "вік": 61, "вага": "65 кг", "група_крові": "B+ "},   # дублікат id=3
    {"id": 4, "вік": 29, "вага": "58 кг", "група_крові": "AB+"},
    {"id": 5, "вік": 250, "вага": "77 кг", "група_крові": "a+"},  # 250 років — очевидний викид
])

# 1. пропуски
patients["вік"].isna().sum()                                  # 1
patients["вік"].fillna(patients["вік"].median(), inplace=True)

# 2. дублікати
patients.duplicated(subset=["id"]).sum()                      # 1
patients.drop_duplicates(subset=["id"], inplace=True)

# 3. типи даних
patients["вага"] = patients["вага"].str.replace(" кг", "").astype(float)

# 4. узгодження категорій
patients["група_крові"] = patients["група_крові"].str.strip().str.upper()

# 5. викиди (IQR)
q1, q3 = patients["вік"].quantile([0.25, 0.75])
iqr = q3 - q1
lower, upper = q1 - 1.5 * iqr, q3 + 1.5 * iqr
patients[(patients["вік"] < lower) | (patients["вік"] > upper)]   # вік=250 — потрапляє в межу
```

Зверніть увагу на порядок дій: спочатку типи й узгодження категорій,
і лише потім пошук викидів — доки `"вага"` зберігається як рядок
`"70 кг"`, порахувати квантилі для неї неможливо.

## Завдання 1. Побудова брудного набору

Побудуйте `DataFrame` із **8 замовлень** для товару свого варіанта
(використовуючи базову ціну й кількість із таблиці), в якому явно
присутні всі перелічені дефекти:

1. 2 рядки з пропущеним значенням (`NaN`) у стовпці `"кількість"`;
2. 1 рядок, що повністю повторює інший (дублікат);
3. щонайменше 2 значення в стовпці `"ціна"` записані як **рядок** із
   суфіксом `" грн"` (наприклад, `"850 грн"`), а не число;
4. назва вашого міста, записана **трьома різними варіантами** в різних
   рядках (наприклад, звичайний варіант, з пробілами навколо, і
   ВЕЛИКИМИ ЛІТЕРАМИ чи латиницею);
5. одне значення `"ціна"`, яке в 8–10 разів перевищує базову ціну
   (умовна помилка вводу або реальний великий замовлення — вирішите на
   Завданні 5).

## Завдання 2. Пропуски

1. Визначте кількість пропусків у кожному стовпці.
2. Заповніть пропуски в `"кількість"` медіаною і поясніть письмово, чому
   медіана тут доречніша за середнє (підказка: у вас усього 8
   спостережень).

## Завдання 3. Дублікати

1. Знайдіть дублікати спочатку без `subset` (повний збіг рядка), потім
   із `subset`, що визначає замовлення унікальним за розумним набором
   полів (наприклад, датою й товаром, якщо вони в вас є, або за самими
   значеннями рядка).
2. Видаліть дублікат і зафіксуйте, скільки рядків залишилось.

## Завдання 4. Типи даних і узгодження категорій

1. Приведіть стовпець `"ціна"` до числового типу (`float`), прибравши
   суфікс `" грн"`.
2. Приведіть усі варіанти назви міста до одного канонічного запису
   (`str.strip()`, `str.lower()` або `.replace()` з явним словником
   заміни) і перевірте результат через `.unique()`.

## Завдання 5. Викиди

1. Знайдіть межі IQR для стовпця `"ціна"` вашого очищеного набору й
   визначте, чи потрапляє в них те велике значення з Завдання 1.
2. Письмово поясніть, яке з двох рішень ви б обрали для цього
   конкретного значення — вважати помилкою вводу (наприклад, зайвий
   нуль) чи реальним великим замовленням — і чому саме таке рішення, а
   не інше.

## Що здати й оцінюється

Здайте ноутбук (`.ipynb`) із зазначеним номером варіанта (містом),
побудованим брудним набором, виконаними Завданнями 1–5 і короткими
письмовими відповідями на три питання:

- Чи можете пояснити, чому заповнення пропуску середнім (а не медіаною)
  може згодом спотворити стандартне відхилення, яке рахуватимемо в
  наступних лекціях?
- Чи розумієте різницю між `duplicated()` без `subset` і з `subset` —
  чому результат може відрізнятись?
- Чи можете пояснити, чому автоматичне видалення всіх значень поза
  межами IQR не завжди правильне рішення?

Оцінюється те, що всі п'ять дефектів справді присутні у вихідному
наборі й коректно усунені саме для вашого варіанта, а також повнота
письмових відповідей.

## Підсумок заняття

- Побудова набору з навмисними дефектами дала змогу побачити кожен тип
  проблеми (пропуск, дублікат, тип, категорія, викид) окремо, а не
  здогадуватись про нього в чужому готовому наборі.
- Виявлення й усунення пропусків, дублікатів, неправильних типів і
  неузгоджених категорій — застосовані послідовно, у порядку, що має
  значення (типи й категорії — перед пошуком викидів).
- Формальний критерій IQR застосований для позначення підозрілого
  значення, а рішення про нього прийняте свідомо, а не автоматично.

**Лекція до цієї практики:** [Лекція 4. Чищення даних](../lectures/04-cleaning.md)
