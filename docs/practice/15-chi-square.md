# Практика 15. Критерії узгодженості й таблиці спряженості: критерій хі-квадрат

*Мета заняття: побудувати таблицю спряженості (`pd.crosstab()`, Лекція 7)
із двох категоріальних змінних власного варіанта, перевірити критерієм
хі-квадрат, чи вони незалежні (Лекція 15), і перевірити критерієм
узгодженості, чи розподіл однієї категоріальної змінної відповідає
заявленій пропорції. Обидва тести застосовуються не до готового
демонстраційного набору, а до даних, які студент сам генерує за
формулою свого варіанта, — тож і результат (пов'язані змінні чи ні)
для кожного варіанта свій, а не наперед відомий з прикладу.*

## Варіанти

Дані варіанта — той самий кліматичний набір, що й у Практиці 8 (свій
рядок за таблицею нижче), розширений двома похідними категоріальними
змінними: **сезон** (зима/весна/літо/осінь за номером місяця) і
**відхилення_від_норми** (категорія "холодніше", "звичайно" чи
"тепліше" — залежно від того, наскільки температура місяця відхиляється
від середньорічної температури вашого варіанта). Саме зв'язок (чи його
відсутність) між сезоном і відхиленням від норми перевірятиметься
критерієм незалежності.

| Варіант | Місто | Середньорічна температура (°C) | Сезонна амплітуда (°C) |
|---|---|---|---|
| 1 | Київ | 9.5 | 14 |
| 2 | Львів | 8.5 | 12 |
| 3 | Одеса | 11.5 | 13 |
| 4 | Харків | 8.8 | 15 |
| 5 | Дніпро | 10.0 | 14 |
| 6 | Чернігів | 8.0 | 13 |
| 7 | Івано-Франківськ | 7.8 | 12 |
| 8 | Полтава | 8.5 | 14 |
| 9 | Суми | 7.5 | 14 |
| 10 | Ужгород | 10.5 | 11 |

Зверніть увагу, що обидві похідні змінні виводяться з **однієї й тієї
самої** формули (сезонного коливання температури навколо
середньорічної), тож апріорі очікувано, що зв'язок між ними в цьому
конкретному наборі **буде** знайдений — саме через те, як побудований
код нижче: "тепліше" і "холодніше" за визначенням прив'язані до
відхилення від середньорічної, а це відхилення й задає сезон. Це не
проблема завдання, а корисний навчальний факт: результат критерію
незалежності варто передбачити, а потім перевірити, чи реальний
p-value дійсно підтверджує очікування, а не просто прийняти будь-яке
число як правильну відповідь.

**Код побудови набору** (підставте свої значення — це лише генерація
вихідних даних, самі завдання нижче виконуються окремо):

```python
import numpy as np
import pandas as pd

np.random.seed(42)
base_temp = ...     # середньорічна температура вашого варіанта
amplitude = ...     # сезонна амплітуда вашого варіанта
city = "..."

def season(month):
    if month in (12, 1, 2):
        return "зима"
    if month in (3, 4, 5):
        return "весна"
    if month in (6, 7, 8):
        return "літо"
    return "осінь"

rows = []
for year in [2021, 2022, 2023, 2024]:
    for month in range(1, 13):
        seasonal = amplitude * np.cos((month - 7) / 12 * 2 * np.pi)
        noise = np.random.normal(0, 1.0)
        temp = round(base_temp + seasonal + noise, 1)
        diff = temp - base_temp
        if diff < -3:
            norm_cat = "холодніше"
        elif diff > 3:
            norm_cat = "тепліше"
        else:
            norm_cat = "звичайно"
        rows.append({
            "місто": city, "рік": year, "місяць": month,
            "температура": temp, "сезон": season(month),
            "відхилення_від_норми": norm_cat,
        })

climate = pd.DataFrame(rows)   # 48 рядків, 4 роки x 12 місяців
```

## Приклад: наскрізна демонстрація

Розберіть цей приклад — **не** копіювати на свій варіант, дані тут
демонстраційні: опитування 200 відвідувачів кав'ярні про улюблений
напій і час дня відвідування.

```python
import pandas as pd
from scipy.stats import chi2_contingency, chisquare

np.random.seed(0)
survey = pd.DataFrame({
    "напій": np.random.choice(
        ["кава", "чай", "какао"], size=200, p=[0.55, 0.30, 0.15]),
    "час_дня": np.random.choice(
        ["ранок", "день", "вечір"], size=200, p=[0.4, 0.35, 0.25]),
})

# трохи "підкрутимо" зв'язок штучно: вранці какао беруть рідше
survey.loc[(survey["час_дня"] == "ранок") & (survey["напій"] == "какао"),
           "напій"] = "кава"

# таблиця спряженості
table = pd.crosstab(survey["час_дня"], survey["напій"])
print(table)

# критерій незалежності
chi2, pvalue, dof, expected = chi2_contingency(table)
print(f"chi2={chi2:.2f}, p-value={pvalue:.4f}, dof={dof}")
print(pd.DataFrame(expected, index=table.index, columns=table.columns).round(1))
```

Інтерпретація: якщо `pvalue` менше за обраний рівень значущості
(наприклад, 0.05), H0 "час дня і напій незалежні" відхиляється — вибір
напою пов'язаний із часом дня. Порівняння `table` (фактичні частоти) з
надрукованою таблицею `expected` показує, у якій клітинці розбіжність
найбільша — саме там, де вранці штучно зменшено частку "какао", і слід
очікувати найбільший внесок у статистику χ².

Критерій узгодженості для тієї самої змінної "напій" проти заявленої в
коді пропорції 55%/30%/15%:

```python
observed_counts = survey["напій"].value_counts().reindex(["кава", "чай", "какао"])
n = len(survey)
expected_counts = [0.55 * n, 0.30 * n, 0.15 * n]

result = chisquare(f_obs=observed_counts, f_exp=expected_counts)
print(result.statistic, result.pvalue)
```

Тут H0 — "справжній розподіл напоїв відповідає пропорції 55/30/15" — і
через штучне втручання вище (переведення частини "какао" в "кава")
фактичний розподіл трохи зсунутий від заявленого; результат тесту
залежить від того, наскільки цей зсув великий порівняно з випадковим
коливанням на вибірці з 200 спостережень.

## Завдання 1. Таблиця спряженості власного варіанта

Побудуйте `pd.crosstab()` для стовпців `"сезон"` і
`"відхилення_від_норми"` вашого набору `climate`. Виведіть таблицю й
письмово опишіть, яка комбінація (наприклад, "літо" і "тепліше")
трапляється найчастіше і чи це узгоджується з інтуїцією про те, як
побудований набір даних. Додатково побудуйте той самий `crosstab()` з
параметром `normalize="index"` і поясніть словами, що змінилося в
таблиці: чому частки в кожному рядку тепер сумуються до 1, і чим це
зручніше для порівняння сезонів між собою, ніж сирі кількості.

## Завдання 2. Критерій незалежності

Застосуйте `chi2_contingency()` до таблиці з Завдання 1. Виведіть
статистику χ², p-value і матрицю `expected`. При рівні значущості
α = 0.05 письмово сформулюйте висновок: чи є підстави вважати сезон і
відхилення від норми пов'язаними в цьому наборі, чи ні. Порівняйте
`observed` і `expected` й вкажіть, у якій клітинці розбіжність
найбільша.

## Завдання 3. Перевірка умови застосовності

Перевірте матрицю `expected` із Завдання 2: чи всі очікувані частоти
клітинок ≥ 5. Якщо ні — письмово опишіть, які категорії варто об'єднати
(наприклад, об'єднати "холодніше" з "звичайно" в одну категорію "не
тепліше"), перебудуйте `crosstab()` і повторіть тест на об'єднаних
категоріях.

## Завдання 4. Критерій узгодженості для сезонів

Порахуйте частоту кожного сезону у вашому наборі `climate["сезон"]`
(`value_counts()`). Оскільки в кожному сезоні по 3 місяці з 12, заявлена
(очікувана) пропорція — по 25% на кожен із чотирьох сезонів. Застосуйте
`chisquare()`, щоб перевірити, чи фактичний розподіл сезонів у ваших
48 рядках узгоджується з рівномірною чвертю на кожен сезон. Письмово
поясніть отриманий результат — очікуваний він чи ні, зважаючи на те, як
побудований код генерації набору.

## Завдання 5. Власна заявлена пропорція

Придумайте власне обґрунтоване припущення про пропорцію категорій
`"відхилення_від_норми"` у вашому наборі (наприклад, "звичайно" — 60%,
"тепліше" і "холодніше" — по 20%), явно запишіть це як H0 у текстовій
клітинці ноутбука, застосуйте `chisquare()` з відповідним `f_exp` і
письмово інтерпретуйте p-value відносно вашого припущення. Додатково
поясніть словами, чому саме ваша заявлена пропорція є розумним
припущенням для перевірки, а не довільним набором чисел (наприклад,
спираючись на межу ±3°C, використану в коді генерації даних вище).

## Що здати й оцінюється

Здайте ноутбук (`.ipynb`) із зазначеним номером варіанта (містом),
побудованим набором `climate` із похідними стовпцями `"сезон"` і
`"відхилення_від_норми"`, виконаними Завданнями 1–5 і короткими
письмовими відповідями на контрольні питання:

- Чи можете пояснити механіку статистики χ² — чому в ній саме квадрат
  відхилення й саме ділення на очікувану частоту?
- Чи можете пояснити, як обчислюється очікувана частота клітинки
  таблиці спряженості (`рядок × стовпець / загальна сума`) і чому саме
  ця формула коректна під припущенням незалежності?
- Чи розумієте різницю між "p-value велике — немає підстав відхилити
  H0" і "p-value велике — доведено, що змінні незалежні"?
- Що робити, якщо очікувана частота в якійсь клітинці таблиці
  спряженості менша за 5, і чому це проблема?

Оцінюється коректність побудови таблиці спряженості й обох тестів саме
на власному варіанті (а не на прикладі з опитуванням), а також
повнота письмової інтерпретації p-value в кожному завданні — сам
факт запуску функції без пояснення результату не вважається виконаним
завданням. Окремо оцінюється Завдання 3: перевірка умови застосовності
не має бути формальною відміткою "перевірено", а має явно показувати,
які саме клітинки не досягають порогу 5 (якщо такі є) і як об'єднання
категорій змінює висновок тесту.

## Підсумок заняття

- Побудовано таблицю спряженості `pd.crosstab()` із двох похідних
  категоріальних змінних власного кліматичного набору.
- Критерій незалежності хі-квадрат (`chi2_contingency`) застосований до
  цієї таблиці, з явним порівнянням фактичних і очікуваних частот і
  висновком про наявність чи відсутність зв'язку.
- Критерій узгодженості хі-квадрат (`chisquare`) застосований до
  розподілу однієї категоріальної змінної — як проти "природної"
  рівномірної пропорції (сезони), так і проти власноруч обґрунтованого
  припущення.
- Перевірено умову застосовності (очікувана частота ≥ 5) і, за потреби,
  застосовано об'єднання категорій як спосіб її відновити.

**Лекція до цієї практики:** [Лекція 15. Критерії узгодженості й перевірка нормальності](../lectures/15-goodness-of-fit.md)
